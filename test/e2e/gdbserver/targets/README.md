# Test firmware targets

Each directory holds the target configuration and deterministic test firmware
for one physical board model. Reusable scenario definitions belong in
`../scenarios/`, and reusable host runner code belongs in `../runner/`; neither
belongs in an individual target directory.
